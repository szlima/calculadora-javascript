const estadoInicial= {
    acumulado: '', 
    atual: '0'
};

export default function calculadoraReducer(state=estadoInicial, action){
  
    switch(action.type){
        
        
        default:
            return state;
    }  
}